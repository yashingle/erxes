import { FormStyle } from 'erxes-ui-utils';

const Label = FormStyle.Label;
const Formgroup = FormStyle.Formgroup;
const Input = FormStyle.Input;
const SelectWrapper = FormStyle.SelectWrapper;
const Select = FormStyle.Select;
const TextArea = FormStyle.TextArea;
const FormLabel = FormStyle.FormLabel;
const Radio = FormStyle.Radio;
const Checkbox = FormStyle.Checkbox;
const Error = FormStyle.Error;
const FlexWrapper = FormStyle.FlexWrapper;

export {
  Input,
  SelectWrapper,
  Select,
  TextArea,
  Radio,
  Checkbox,
  FormLabel,
  Label,
  Formgroup,
  FlexWrapper,
  Error
};
